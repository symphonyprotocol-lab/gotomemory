{
  "name": "@gotomemory/gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Memory Gateway API — HTTP entry point over the orchestrator (system spec §7.1).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@gotomemory/audit": "workspace:*",
    "@gotomemory/contracts": "workspace:*",
    "@gotomemory/core": "workspace:*",
    "@gotomemory/crypto": "workspace:*",
    "@gotomemory/db": "workspace:*",
    "fastify": "^5.1.0",
    "yaml": "^2.9.0"
  }
}
