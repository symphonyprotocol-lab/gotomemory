{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "entrypoints/**", "wxt.config.ts", "package.json"],
      "outputs": [".output/**"]
    }
  }
}
