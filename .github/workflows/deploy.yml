# Production deploy for gotomemory.dev.
#
# Triggered by `ci` COMPLETING on main rather than by the push itself: merging a
# PR starts both workflows at once, so a plain `on: push` deploy would race the
# tests and could ship a commit whose suite went red. This way a merge deploys
# only after its own CI run succeeded.
#
# Trade-off to know about: if ci.yml is renamed, or is skipped for a commit, no
# deploy fires and nothing complains. Use the manual run below in that case.
#
# Reacting to `workflow_run` also means reacting to ci runs from fork pull
# requests, and this repo is public, which is why the publish job's `if` is as
# specific as it is — read the comment there before loosening it.
#
# Who can ship: the `DEPLOY_OWNER` repository variable holds the one account
# allowed to trigger a production deploy. Unset it and nothing deploys, which
# is the right way for this to fail.

name: deploy

on:
  workflow_run:
    workflows: [ci]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      ref:
        description: Commit or branch to deploy
        required: false
        default: main

permissions:
  contents: read

concurrency:
  # Never let two deploys interleave on the server; queue instead of cancelling,
  # so an in-flight `docker compose up` is not killed halfway.
  group: deploy-production
  cancel-in-progress: false

jobs:
  publish:
    # Production ships only when DEPLOY_OWNER asked for it, one way or another.
    #
    # A manual run deploys whatever ref was asked for. `workflow_dispatch`
    # already needs write access on the repo, and the actor check narrows that
    # from "any collaborator" to one person.
    #
    # An automatic run has to clear four checks, because this repo is public
    # and `ci` also runs on pull requests, including ones from forks. A
    # `workflow_run` job gets the real secrets even when the run that triggered
    # it did not, so an unguarded reaction to "some ci run finished" would hand
    # DEPLOY_SSH_KEY to whatever commit that run was built from:
    #   conclusion  — the suite it is reacting to actually passed.
    #   event       — it was a push to main, not a pull request. This is the
    #                 load-bearing one against forks: `branches: [main]` above
    #                 filters on the *head* branch, which for a fork PR is the
    #                 contributor's own branch name, so a fork branch called
    #                 `main` satisfies it. Pushing to this repo's main needs
    #                 write access.
    #   head_repo   — the commit came from this repo, not from a fork.
    #   actor       — DEPLOY_OWNER is the one who pushed it.
    #
    # Consequence to know about: if another collaborator merges to main, CI
    # runs and no deploy follows, silently. That is the intended behaviour —
    # ship it yourself with the manual run above.
    #
    # This check stops an unwanted deploy, not a determined collaborator: write
    # access includes editing this file. The `production` environment's
    # required-reviewer rule is the part that cannot be edited away from here.
    if: >-
      (github.event_name == 'workflow_dispatch' &&
      github.actor == vars.DEPLOY_OWNER) ||
      (github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_repository.full_name == github.repository &&
      github.event.workflow_run.actor.login == vars.DEPLOY_OWNER)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.name.outputs.image }}
    steps:
      - name: Resolve the commit being deployed
        id: sha
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "ref=${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ inputs.ref }}" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.sha.outputs.ref }}

      # Tag by SHA, not `latest`: the server then runs an identifiable build and
      # a rollback is "point .env at the previous SHA".
      - name: Compose the image reference
        id: name
        run: |
          sha="$(git rev-parse --short HEAD)"
          echo "image=ghcr.io/${{ github.repository }}-web:${sha}" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push the site image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          tags: ${{ steps.name.outputs.image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Attach a GitHub Environment so the deploy secrets live behind it and can
    # be given required reviewers later without touching this file.
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || inputs.ref }}

      - name: Configure SSH
        env:
          KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          printf '%s\n' "$KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          # Pinned host keys rather than StrictHostKeyChecking=no: the deploy key
          # is a write credential, and disabling the check hands it to anyone who
          # can answer on that address.
          printf '%s\n' "$KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Ship the compose files
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          PORT: ${{ secrets.DEPLOY_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          ssh -i ~/.ssh/deploy_key -p "$PORT" "$USER@$HOST" \
            "mkdir -p '$DEPLOY_PATH/selector-config'"
          scp -i ~/.ssh/deploy_key -P "$PORT" \
            deploy/docker-compose.yml deploy/config-endpoint.nginx.conf \
            "$USER@$HOST:$DEPLOY_PATH/"

      - name: Pull and restart
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          PORT: ${{ secrets.DEPLOY_PORT || '22' }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          IMAGE: ${{ needs.publish.outputs.image }}
          # Only needed while the GHCR package is private; leave the secret unset
          # once the package is public and this step skips the login.
          GHCR_PULL_TOKEN: ${{ secrets.GHCR_PULL_TOKEN }}
          GHCR_PULL_USER: ${{ secrets.GHCR_PULL_USER || github.repository_owner }}
        run: |
          ssh -i ~/.ssh/deploy_key -p "$PORT" "$USER@$HOST" \
            IMAGE="$IMAGE" \
            DEPLOY_PATH="$DEPLOY_PATH" \
            GHCR_PULL_TOKEN="$GHCR_PULL_TOKEN" \
            GHCR_PULL_USER="$GHCR_PULL_USER" \
            'bash -euo pipefail -s' <<'REMOTE'
            cd "$DEPLOY_PATH"

            if [ -n "$GHCR_PULL_TOKEN" ]; then
              printf '%s' "$GHCR_PULL_TOKEN" | docker login ghcr.io -u "$GHCR_PULL_USER" --password-stdin
            fi

            # .env is what pins the running version; keep the previous value so a
            # rollback does not need a GitHub round-trip.
            [ -f .env ] && cp .env .env.previous
            printf 'WEB_IMAGE=%s\n' "$IMAGE" > .env
            # The config container mounts config-endpoint.nginx.conf, and compose
            # cannot see that a mounted file's contents changed — it would leave
            # the old nginx process running with the config it started with.
            # Feeding the checksum in as an environment variable makes the change
            # visible to compose, so the container is recreated exactly when the
            # file actually differs.
            printf 'CONFIG_CONF_HASH=%s\n' \
              "$(sha256sum config-endpoint.nginx.conf | cut -d' ' -f1)" >> .env

            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
          REMOTE

      - name: Verify both services answer
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          PORT: ${{ secrets.DEPLOY_PORT || '22' }}
          WEB_PORT: ${{ secrets.DEPLOY_WEB_PORT || '9280' }}
          CONFIG_PORT: ${{ secrets.DEPLOY_CONFIG_PORT || '9281' }}
        # A deploy that starts a broken container should fail loudly here rather
        # than look green. /privacy/ is checked because the store listing links
        # it, and the config endpoint because it was previously unchecked — which
        # is how a deploy that never applied a new config-endpoint.nginx.conf
        # reported success while serving the old one.
        run: |
          ssh -i ~/.ssh/deploy_key -p "$PORT" "$USER@$HOST" \
            WEB_PORT="$WEB_PORT" CONFIG_PORT="$CONFIG_PORT" \
            'bash -euo pipefail -s' <<'REMOTE'
            check() {
              local url="$1"
              local code=""
              for attempt in 1 2 3 4 5; do
                code="$(curl -s -o /dev/null -w '%{http_code}' "$url" || true)"
                [ "$code" = "200" ] && return 0
                sleep 3
              done
              echo "::error::${url} returned ${code} after the restart"
              return 1
            }

            check "http://127.0.0.1:${WEB_PORT}/"
            check "http://127.0.0.1:${WEB_PORT}/privacy/"
            check "http://127.0.0.1:${CONFIG_PORT}/"
          REMOTE
