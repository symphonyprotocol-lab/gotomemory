{
  "name": "@gotomemory/audit",
  "version": "0.0.0",
  "private": true,
  "description": "Append-only audit sink with a tamper-evident hash chain (system spec §7.6, §8.6).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
