{
  "name": "@gotomemory/db",
  "version": "0.0.0",
  "private": true,
  "description": "Storage layer: memory repository interface, in-memory implementation, and SQL migrations (system spec §8).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "migrations"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
