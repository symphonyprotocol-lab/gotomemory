{
  "name": "@gotomemory/adapters",
  "version": "0.0.0",
  "private": true,
  "description": "Platform adapters: payload strategies and versioned capability manifests (system spec §7.3).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
