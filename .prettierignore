# Prose specs are authored by hand, not Prettier-formatted.
specs/

# Python workspace — formatted by Ruff, not Prettier.
py/

# Build output, caches, committed codegen artifacts.
dist/
.turbo/
coverage/
**/generated/
pnpm-lock.yaml

# Playwright run artifacts. Git-ignored, but Prettier still walked them, so
# `pnpm run check` failed for anyone who had run the e2e suite locally.
**/test-results/
**/playwright-report/
