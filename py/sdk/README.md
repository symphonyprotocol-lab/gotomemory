# gotomemory Python SDK

> **FROZEN (2026-07)** — per monorepo spec §13 (`specs/monorepo-architecture.md`), this
> SDK is frozen pending validation of product spec §12.4
> (`specs/memory-sharing-system.md`): no releases, no new features, CI demoted to the
> nightly `frozen-nightly` workflow, fatal fixes only. Run its tests locally with
> `cd py && uv run pytest -q` (root `pnpm run py:test`).

Python SDK for developer and agent integrations.
