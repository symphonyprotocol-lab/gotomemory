[project]
name = "gotomemory"
version = "0.0.0"
description = "Python SDK for the gotomemory Gateway API (system spec §16.3)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
